# WSL Quick Commands

```pwsh
wsl --status
wsl --list --verbose
wsl --shutdown
wsl -d Ubuntu
```bash

Inside WSL:

```bash
uname -a
pwd
ls -la
sudo apt update
```bash

Interop:

```bash
explorer.exe .
code .
```bash
