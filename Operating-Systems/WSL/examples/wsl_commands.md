# WSL Quick Commands

```pwsh
wsl --status
wsl --list --verbose
wsl --shutdown
wsl -d Ubuntu
```

Inside WSL:

```bash
uname -a
pwd
ls -la
sudo apt update
```

Interop:

```bash
explorer.exe .
code .
```
