# TensorFlow User Guide

## Install (Python)

```bash
python -m pip install --upgrade pip
pip install tensorflow
```bash

Verify:

```bash
python -c "import tensorflow as tf; print(tf.__version__)"
```bash

## Minimal Keras example

See `TensorFlow/examples/linear_regression.py`.

## Notes

- GPU setup depends on your OS and hardware and can be more complex.
- Start with CPU-only for learning.
