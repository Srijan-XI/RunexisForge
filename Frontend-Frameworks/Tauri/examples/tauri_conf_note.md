# Tauri config note

Tauri apps include a config file (often `tauri.conf.json` in older setups).

When you create a project using `npm create tauri-app@latest`, use the generated config as the canonical reference.
