terraform {
  required_version = ">= 1.5.0"

  required_providers {
    # Replace with a real provider when you want to run it.
    # Example:
    # aws = {
    #   source  = "hashicorp/aws"
    #   version = "~> 5.0"
    # }
  }
}

# This example is intentionally minimal and safe-by-default.
# Add a provider + resources when you are ready.
