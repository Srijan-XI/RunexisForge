```python
import { describe, expect, it } from "vitest";

// Q1: Write tests
//
// Create a function `isEven(n: number): boolean` and write tests for:
// - 0 is even
// - 1 is not even
// - 2 is even

function isEven(n: number): boolean {
  // TODO
  return false;
}

describe("isEven", () => {
  it("handles basic cases", () => {
    // TODO
    expect(isEven(0)).toBe(true);
  });
});
```python
